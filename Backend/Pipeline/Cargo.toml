[package]
name = "Pipeline"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] } #*Well this for data cleansing
serde_json = "1.0"  #*More functionality to the serde crate
tokio = {version = "1", features = ["full"]} #*Gives us tokio runtime